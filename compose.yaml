name: ngencerf-ui

services:
  ngencerf-app:
    image: registry.sh.nextgenwaterprediction.com/ngwpc/nwm-ngen/ngencerf_ui:latest
    pull_policy: build
    build: 
      context: .
    ports:
      - "3000:3000"
    environment:
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000

