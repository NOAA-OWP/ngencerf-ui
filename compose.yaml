name: ngencerf-ui

services:
  ngencerf-app:
    build: 
      context: .
      secrets:
        - gitlab_token
    ports:
      - "3000:3000"
    environment:
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000

secrets:
  gitlab_token:
    file: ~/.gitlab_token
